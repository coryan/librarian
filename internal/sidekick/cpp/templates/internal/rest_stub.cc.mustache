// Copyright {{Codec.CopyrightYear}} Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Generated by the Codegen C++ plugin.
// If you make any local changes, they will be lost.
// source: {{Codec.ProtoSourceFile}}

#include "{{Codec.RestStubHeaderPath}}"
#include "google/cloud/common_options.h"
#include "google/cloud/internal/absl_str_cat_quiet.h"
#include "google/cloud/internal/rest_stub_helpers.h"
#include "google/cloud/status_or.h"
{{#Codec.RestStubCcProtobufIncludes}}
#include <{{.}}>
{{/Codec.RestStubCcProtobufIncludes}}
#include <memory>
#include <utility>

namespace google {
namespace cloud {
namespace {{Codec.InternalNamespace}} {
GOOGLE_CLOUD_CPP_INLINE_NAMESPACE_BEGIN

{{#Codec.HasLRO}}
{{Codec.DefaultRestStubClassName}}::{{Codec.DefaultRestStubClassName}}(Options options)
    : service_(rest_internal::MakePooledRestClient(
          options.get<EndpointOption>(), options)),
      operations_(rest_internal::MakePooledRestClient(
{{#Codec.HasGrpcLRO}}
          options.get<rest_internal::LongrunningEndpointOption>(), options)),
{{/Codec.HasGrpcLRO}}
{{^Codec.HasGrpcLRO}}
          options.get<EndpointOption>(), options)),
{{/Codec.HasGrpcLRO}}
      options_(std::move(options)) {}

{{Codec.DefaultRestStubClassName}}::{{Codec.DefaultRestStubClassName}}(
    std::shared_ptr<rest_internal::RestClient> service,
    std::shared_ptr<rest_internal::RestClient> operations,
    Options options)
    : service_(std::move(service)),
      operations_(std::move(operations)),
      options_(std::move(options)) {}
{{/Codec.HasLRO}}
{{^Codec.HasLRO}}
{{Codec.DefaultRestStubClassName}}::{{Codec.DefaultRestStubClassName}}(Options options)
    : service_(rest_internal::MakePooledRestClient(
          options.get<EndpointOption>(), options)),
      options_(std::move(options)) {}

{{Codec.DefaultRestStubClassName}}::{{Codec.DefaultRestStubClassName}}(
    std::shared_ptr<rest_internal::RestClient> service,
    Options options)
    : service_(std::move(service)),
      options_(std::move(options)) {}
{{/Codec.HasLRO}}
{{#Codec.RestMethods}}
{{#IsLRO}}

future<StatusOr<{{{LroOperationType}}}>>
{{Codec.DefaultRestStubClassName}}::Async{{Name}}(
      CompletionQueue& cq,
      std::unique_ptr<rest_internal::RestContext> rest_context,
      google::cloud::internal::ImmutableOptions options,
      {{RequestType}} const& request) {
  promise<StatusOr<{{{LroOperationType}}}>> p;
  future<StatusOr<{{{LroOperationType}}}>> f = p.get_future();
  std::thread t{[](
          auto p, auto service, auto request, auto rest_context, auto options) {
      std::vector<std::pair<std::string, std::string>> query_params;{{{RestQueryParamsCode}}}
      p.set_value(rest_internal::{{RestHttpVerb}}<{{{LroOperationType}}}>(
          *service, *rest_context, {{RestRequestResource}},
          false,
          {{{RestPathAsync}}}, std::move(query_params)));
    },
    std::move(p), service_, request, std::move(rest_context),
    std::move(options)};
  return f.then([t = std::move(t), cq](auto f) mutable {
    cq.RunAsync([t = std::move(t)]() mutable {
      t.join();
    });
    return f.get();
  });
}

StatusOr<{{{LroOperationType}}}>
{{Codec.DefaultRestStubClassName}}::{{Name}}(
      google::cloud::rest_internal::RestContext& rest_context,
      Options const& options,
      {{RequestType}} const& request) {
  std::vector<std::pair<std::string, std::string>> query_params;{{{RestQueryParamsCode}}}
  return rest_internal::{{RestHttpVerb}}<{{{LroOperationType}}}>(
      *service_, rest_context, {{RestRequestResource}}, false,
      {{{RestPathSync}}}, std::move(query_params));
}
{{/IsLRO}}
{{^IsLRO}}
{{#IsVoid}}

Status {{Codec.DefaultRestStubClassName}}::{{Name}}(
      google::cloud::rest_internal::RestContext& rest_context,
      Options const& options,
      {{RequestType}} const& request) {
  std::vector<std::pair<std::string, std::string>> query_params;{{{RestQueryParamsCode}}}
  return rest_internal::{{RestHttpVerb}}<google::cloud::rest_internal::EmptyResponseType>(
      *service_, rest_context, {{RestRequestResource}}, false,
      {{{RestPathSync}}}, std::move(query_params));
}
{{/IsVoid}}
{{^IsVoid}}

StatusOr<{{ResponseType}}>
{{Codec.DefaultRestStubClassName}}::{{Name}}(
      google::cloud::rest_internal::RestContext& rest_context,
      Options const& options,
      {{RequestType}} const& request) {
  std::vector<std::pair<std::string, std::string>> query_params;{{{RestQueryParamsCode}}}
  return rest_internal::{{RestHttpVerb}}<{{ResponseType}}>(
      *service_, rest_context, {{RestRequestResource}}, false,
      {{{RestPathSync}}}, std::move(query_params));
}
{{/IsVoid}}
{{/IsLRO}}
{{/Codec.RestMethods}}
{{#Codec.RestAsyncMethods}}
{{#IsVoid}}

future<Status>
{{Codec.DefaultRestStubClassName}}::Async{{Name}}(
    google::cloud::CompletionQueue& cq,
    std::unique_ptr<rest_internal::RestContext> rest_context,
    google::cloud::internal::ImmutableOptions options,
    {{RequestType}} const& request) {
  promise<StatusOr<google::protobuf::Empty>> p;
  future<StatusOr<google::protobuf::Empty>> f = p.get_future();
  std::thread t{[](
          auto p, auto service, auto request, auto rest_context, auto options) {
      std::vector<std::pair<std::string, std::string>> query_params;{{{RestQueryParamsCode}}}
      p.set_value(rest_internal::{{RestHttpVerb}}<google::protobuf::Empty>(
          *service, *rest_context, {{RestRequestResource}}, false,
          {{{RestPathAsync}}}, std::move(query_params)));
    },
    std::move(p), service_, request, std::move(rest_context),
    std::move(options)};
  return f.then([t = std::move(t), cq](auto f) mutable {
    cq.RunAsync([t = std::move(t)]() mutable {
      t.join();
    });
    return f.get().status();
  });
}
{{/IsVoid}}
{{^IsVoid}}

future<StatusOr<{{ResponseType}}>>
{{Codec.DefaultRestStubClassName}}::Async{{Name}}(
    google::cloud::CompletionQueue& cq,
    std::unique_ptr<rest_internal::RestContext> rest_context,
    google::cloud::internal::ImmutableOptions options,
    {{RequestType}} const& request) {
  promise<StatusOr<{{ResponseType}}>> p;
  future<StatusOr<{{ResponseType}}>> f = p.get_future();
  std::thread t{[](
          auto p, auto service, auto request, auto rest_context, auto options) {
      std::vector<std::pair<std::string, std::string>> query_params;{{{RestQueryParamsCode}}}
      p.set_value(rest_internal::{{RestHttpVerb}}<{{ResponseType}}>(
          *service, *rest_context, {{RestRequestResource}}, false,
          {{{RestPathAsync}}}, std::move(query_params)));
    },
    std::move(p), service_, request, std::move(rest_context),
    std::move(options)};
  return f.then([t = std::move(t), cq](auto f) mutable {
    cq.RunAsync([t = std::move(t)]() mutable {
      t.join();
    });
    return f.get();
  });
}
{{/IsVoid}}
{{/Codec.RestAsyncMethods}}
{{#Codec.HasLRO}}

future<StatusOr<{{{Codec.LroResponseType}}}>>
{{Codec.DefaultRestStubClassName}}::AsyncGetOperation(
    google::cloud::CompletionQueue& cq,
    std::unique_ptr<rest_internal::RestContext> rest_context,
    google::cloud::internal::ImmutableOptions options,
    {{{Codec.LroGetOperationRequestType}}} const& request) {
  promise<StatusOr<{{{Codec.LroResponseType}}}>> p;
  future<StatusOr<{{{Codec.LroResponseType}}}>> f = p.get_future();
  std::thread t{[](auto p, auto operations, auto request, auto rest_context, auto options) {
      p.set_value(rest_internal::Get<{{{Codec.LroResponseType}}}>(
          *operations, *rest_context, request, false,
          {{{Codec.LroGetOperationPathRest}}}));
    },
    std::move(p), operations_, request, std::move(rest_context),
    std::move(options)};
  return f.then([t = std::move(t), cq](auto f) mutable {
    cq.RunAsync([t = std::move(t)]() mutable {
      t.join();
    });
    return f.get();
  });
}

future<Status>
{{Codec.DefaultRestStubClassName}}::AsyncCancelOperation(
    google::cloud::CompletionQueue& cq,
    std::unique_ptr<rest_internal::RestContext> rest_context,
    google::cloud::internal::ImmutableOptions options,
    {{{Codec.LroCancelOperationRequestType}}} const& request) {
  promise<StatusOr<google::protobuf::Empty>> p;
  future<StatusOr<google::protobuf::Empty>> f = p.get_future();
  std::thread t{[](auto p, auto operations, auto request, auto rest_context, auto options) {
      p.set_value(rest_internal::Post<google::protobuf::Empty>(
          *operations, *rest_context, request, false,
          {{{Codec.LroCancelOperationPathRest}}}));
    },
    std::move(p), operations_, request, std::move(rest_context),
    std::move(options)};
  return f.then([t = std::move(t), cq](auto f) mutable {
    cq.RunAsync([t = std::move(t)]() mutable {
      t.join();
    });
    return f.get().status();
  });
}
{{/Codec.HasLRO}}

GOOGLE_CLOUD_CPP_INLINE_NAMESPACE_END
}  // namespace {{Codec.InternalNamespace}}
}  // namespace cloud
}  // namespace google
