// Copyright {{Codec.CopyrightYear}} Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Generated by the Codegen C++ plugin.
// If you make any local changes, they will be lost.
// source: {{Codec.ProtoSourceFile}}

#ifndef {{Codec.MockConnectionHeaderGuard}}
#define {{Codec.MockConnectionHeaderGuard}}

#include "{{Codec.ConnectionHeaderPath}}"
#include <gmock/gmock.h>

namespace google {
namespace cloud {
namespace {{Codec.MocksNamespace}} {
GOOGLE_CLOUD_CPP_INLINE_NAMESPACE_BEGIN

/**
 * A class to mock `{{Codec.ConnectionClassName}}`.
 *
 * Application developers may want to test their code with simulated responses,
 * including errors, from an object of type `{{Codec.ClientClassName}}`. To do so,
 * construct an object of type `{{Codec.ClientClassName}}` with an instance of this
 * class. Then use the Google Test framework functions to program the behavior
 * of this mock.
 *
 * @see [This example][bq-mock] for how to test your application with GoogleTest.
 * While the example showcases types from the BigQuery library, the underlying
 * principles apply for any pair of `*Client` and `*Connection`.
 *
 * [bq-mock]: @cloud_cpp_docs_link{bigquery,bigquery-read-mock}
 */
class {{Codec.MockConnectionClassName}} : public {{Codec.PackageNamespace}}::{{Codec.ConnectionClassName}} {
 public:
  MOCK_METHOD(Options, options, (), (override));
{{#Codec.Methods}}
{{#IsBidiStreaming}}

  MOCK_METHOD((std::unique_ptr<
      ::google::cloud::AsyncStreamingReadWriteRpc<
          {{RequestType}}, {{ResponseType}}>>),
      Async{{Name}}, (), (override));
{{/IsBidiStreaming}}
{{^IsBidiStreaming}}
{{#IsLRO}}

  /// To disambiguate calls, use:
  ///
  /// @code
  /// using ::testing::_;
  /// using ::testing::Matcher;
  /// EXPECT_CALL(*mock, {{Name}}(Matcher<{{RequestType}} const&>(_)))
  /// @endcode
  MOCK_METHOD({{{LroFutureReturnType}}},
  {{Name}},
  ({{RequestType}} const& request), (override));


  /// To disambiguate calls, use:
  ///
  /// @code
  /// using ::testing::_;
  /// EXPECT_CALL(*mock, {{Name}}(_, _))
  /// @endcode
  MOCK_METHOD({{{LroStartReturnType}}},
  {{Name}}, (NoAwaitTag,
    {{RequestType}} const& request), (override));


  /// To disambiguate calls, use:
  ///
  /// @code
  /// using ::testing::_;
  /// using ::testing::Matcher;
  /// EXPECT_CALL(*mock, {{Name}}(Matcher<google::longrunning::Operation const&>(_)))
  /// @endcode
  MOCK_METHOD({{{LroFutureReturnType}}},
  {{Name}}, (
    google::longrunning::Operation const& operation), (override));
{{/IsLRO}}
{{^IsLRO}}
{{#IsPaginated}}

  MOCK_METHOD((StreamRange<{{{RangeOutputType}}}>),
  {{Name}},
  ({{RequestType}} request), (override));
{{/IsPaginated}}
{{^IsPaginated}}
{{#IsStreamingRead}}

  MOCK_METHOD(StreamRange<{{{ResponseType}}}>,
  {{Name}},
  ({{RequestType}} const& request), (override));
{{/IsStreamingRead}}
{{^IsStreamingRead}}

  MOCK_METHOD({{{PlainReturnType}}},
  {{Name}},
  ({{RequestType}} const& request), (override));
{{/IsStreamingRead}}
{{/IsPaginated}}
{{/IsLRO}}
{{/IsBidiStreaming}}
{{/Codec.Methods}}
{{#Codec.AsyncMethods}}

  MOCK_METHOD(future<{{{PlainReturnType}}}>,
  Async{{Name}},
  ({{RequestType}} const& request), (override));
{{/Codec.AsyncMethods}}
};

GOOGLE_CLOUD_CPP_INLINE_NAMESPACE_END
}  // namespace {{Codec.MocksNamespace}}
}  // namespace cloud
}  // namespace google

#endif  // {{Codec.MockConnectionHeaderGuard}}
